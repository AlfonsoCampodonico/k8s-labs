apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-static
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ""
  resources:
    requests: { storage: 200Mi }
  volumeName: demo-static
