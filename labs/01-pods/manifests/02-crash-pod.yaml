apiVersion: v1
kind: Pod
metadata:
  name: crasher
  labels:
    app: crasher
spec:
  restartPolicy: Always
  containers:
    - name: boom
      image: busybox:1.36
      command: ["sh", "-c", "echo starting; sleep 2; echo bye; exit 1"]
