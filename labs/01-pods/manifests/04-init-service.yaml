apiVersion: v1
kind: Service
metadata:
  name: upstream
spec:
  selector:
    app: upstream
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Pod
metadata:
  name: upstream
  labels:
    app: upstream
spec:
  containers:
    - name: upstream
      image: nginx:1.27-alpine
