apiVersion: v1
kind: Pod
metadata:
  name: broken
  labels: { app: broken }
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo starting; sleep 5; echo exiting; exit 1"]
